import loader from './img/loader.gif'

const Loader = () => {
    return (
        <div className="loader">
            <img src={ loader } alt="" />
        </div>
      );
}
 
export default Loader;